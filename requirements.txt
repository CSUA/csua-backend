Django==2.0.6
ldap3
Pillow
python-magic
python-magic-bin; platform_system == 'Windows'

# OpenLDAP libraries don't have a binary distribution so building and linking
# against them is a big pain on windows. Here's a prebuilt wheel file instead!
# TODO: confirm installation works for Linux and OSX
requirements/python_ldap-3.1.0-cp36-cp36m-win_amd64.whl; platform_system == 'Windows'

django-ldapdb
